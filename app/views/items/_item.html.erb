<div id="<%= dom_id item %>">
<img src= <%= item_image_url(item) %> alt="tech image">

<p><strong><%= item.name %></strong> costs: <%= number_to_currency(item.starting_price, unit: '£') %></p>
<p>Listed by: <%= item.owner&.email || 'unknown' %></p>
</div>
