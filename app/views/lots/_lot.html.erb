<div id="<%= dom_id lot %>">
    <h3>
        <%= lot.item.name %>
    </h3>
    <img src=<%= item_image_url(lot.item) %> style="width:496px;" alt="tech image">
    <p>
        <%= lot.item.description %>
    </p>
    <p>
        <%= lot.notes %>
    </p>
</div>
